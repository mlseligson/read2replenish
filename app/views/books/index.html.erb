<h1>Books#index</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Author</th>
    <th>Published</th>
  </tr>
  <% @books.each do |book| %>
    <tr>
      <td><%= book.title %></td>
      <td><%= book.author %></td>
      <td><%= book.date_published %></td>
    </tr>
  <% end %>
</table>